import React from "react";
import { Layout, Button } from "../../components";

const work = () => {
    return (
        <Layout>
        <div id="work">
            Work
        </div>
        </Layout>
    )
}
export default work;
